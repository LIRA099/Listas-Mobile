let tempoTotal = 0;

for (let degrau = 1; degrau <= 20; degrau++) {

    tempoTotal = tempoTotal + 3;

    console.log(
        "Degrau " +
        degrau +
        ": " +
        tempoTotal +
        " segundos"
    );
}